import common from './common';
import envelope from './Envelope.validator';
import requests from './requests';
import responses from './responses';

export default {
  common,
  envelope,
  requests,
  responses,
};
